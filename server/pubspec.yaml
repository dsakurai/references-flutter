name: server
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo
publish_to: 'none'

environment:
  sdk: ^3.9.0

# Add regular dependencies here.
dependencies:
  mysql1: ^0.20.0
  path: ^1.9.0
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  references_models:
    path: ../references_models

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
